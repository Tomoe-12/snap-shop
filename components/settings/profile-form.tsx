"use client";

import { Form } from "../ui/form";

const ProfileForm = () => {
  return (
    <form>
      <input type="text" />
      <button>close</button>
    </form>
  );
};

export default ProfileForm;
